extends ../layouts/common

block content
    h1 User Info

    img(src= "../" + user.avatar, width='256')

    p
        span id&nbsp;
            b= user.id
            span &nbsp;-&nbsp;name&nbsp; 
            b= user.name
